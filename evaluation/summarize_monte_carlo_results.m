function summary = summarize_monte_carlo_results( ...
        data, metric_names, bootstrap_resamples, seed)
%SUMMARIZE_MONTE_CARLO_RESULTS Summarize successful Monte Carlo trials.

[controllers, metrics] = validate_statistics_data(data, metric_names, ...
    bootstrap_resamples, seed);
summary = build_statistics_summary(data, controllers, metrics, ...
    bootstrap_resamples, seed);
end
